# A题 烟幕干扰弹的投放策略 — 解题分析与整体方案

> 2025年高教社杯全国大学生数学建模竞赛 A题
> 重庆大学2026暑期数学建模第二次模拟赛

---

## 一、题目背景与核心任务

运用长续航无人机挂载烟幕干扰弹，在来袭空地导弹与被保护的真实目标之间形成烟幕遮蔽，避免导弹发现真目标。核心是设计投放策略（无人机航向、速度、投放点、起爆点），使多枚烟幕弹对真目标的有效遮蔽时间尽可能长。

---

## 二、已知条件与坐标系

### 2.1 坐标系
- 原点：假目标位置 (0, 0, 0)
- xy 平面：水平面
- 真目标：圆柱体，半径 7 m，高 10 m，下底面圆心 (0, 200, 0)
  - 占据范围：x∈[-7, 7], y∈[193, 207], z∈[0, 10]

### 2.2 来袭导弹（空地导弹）
| 导弹 | 初始位置 (m) | 到假目标距离 (m) | 飞行时间 (s) |
|------|-------------|-----------------|-------------|
| M1 | (20000, 0, 2000) | 20099.8 | 67.00 |
| M2 | (19000, 600, 2100) | 19125.1 | 63.75 |
| M3 | (18000, -600, 1900) | 18109.9 | 60.37 |

- 飞行速度：300 m/s，方向直指假目标（原点）
- 轨迹：匀速直线 P(t) = P₀ · (1 - 300t / |P₀|)

### 2.3 无人机
| 无人机 | 位置 (m) | 到假目标距离 (m) |
|--------|---------|-----------------|
| FY1 | (17800, 0, 1800) | 17890.8 |
| FY2 | (12000, 1400, 1400) | 12162.2 |
| FY3 | (6000, -3000, 700) | 6744.6 |
| FY4 | (11000, 2000, 1800) | 11324.3 |
| FY5 | (13000, -2000, 1300) | 13217.0 |

- 受领任务后可瞬时调整航向，然后以 70~140 m/s **等高度**匀速直线飞行
- 航向、速度一旦确定不再调整
- 每架无人机投放两枚弹至少间隔 1 s

### 2.4 烟幕干扰弹物理特性
1. **脱离无人机后**：在重力作用下运动（平抛运动）
   - 水平：继承无人机速度（等高度匀速直线）
   - 垂直：自由落体 z(t') = z₀ - ½g(t')²，g = 9.8 m/s²
2. **起爆后瞬时**形成球状烟幕云团
3. **云团动态**：以 3 m/s 匀速下沉
4. **有效遮蔽**：云团中心 10 m 范围内，起爆 20 s 内有效
5. **时间引信**：可精确控制起爆时间

---

## 三、比赛规定与限制

### 3.1 论文格式
- A4 纸，页边距 2.5 cm，PDF 格式提交，≤ 20 MB
- 第一页封面，第二页摘要（≤ 1 页，从第 1 页编页码），正文 ≤ 30 页，附录不限
- 论文题目三号黑体，一级标题四号黑体居中，正文小四号宋体，行距 20 磅
- 摘要占评阅重要权重

### 3.2 AI 工具使用规定
- 不强制使用 AI；未使用须声明"本参赛队未使用任何 AI 工具"
- 使用 AI 须：① 正文标注 AI 生成内容 ② 参考文献列出 AI 工具 ③ 支撑材料含"AI 工具使用详情"PDF
- 核心建模与分析必须独立完成
- 不符合要求取消评奖资格

### 3.3 支撑材料
- 含全部可运行源程序、数据、中间结果图表、AI 使用详情
- 压缩为 RAR/ZIP，≤ 20 MB
- 文件列表放入论文附录

---

## 四、核心物理模型

### 4.1 无人机运动模型
无人机 i 在受领任务后以航向角 θᵢ、速度 vᵢ（70~140 m/s）等高度匀速直线飞行：

```
P_uav(t) = P₀ᵢ + vᵢ · (cosθᵢ, sinθᵢ, 0) · t
```

### 4.2 炸弹弹道模型（平抛运动）
设投放时刻 tᵣ，起爆延时 Δt_f（引信控制），则：

**投放点**（无人机在 tᵣ 时刻位置）：
```
R = P₀ᵢ + vᵢ · (cosθᵢ, sinθᵢ, 0) · tᵣ
```

**起爆点**（投放后 Δt_f 时刻炸弹位置）：
```
D = R + vᵢ·(cosθᵢ, sinθᵢ, 0)·Δt_f + (0, 0, -½g·Δt_f²)
```

### 4.3 烟幕云团模型
起爆时刻 t_d = tᵣ + Δt_f，起爆后云团中心匀速下沉：

```
C(t) = D + (0, 0, -3·(t - t_d)),  t ∈ [t_d, t_d + 20]
```

有效区域：以 C(t) 为球心、半径 10 m 的球体；有效时间窗口 20 s。

### 4.4 导弹轨迹模型
导弹 Mk 飞向假目标（原点），匀速直线：

```
M(t) = P₀k · (1 - 300t / |P₀k|)
```

### 4.5 视线遮蔽判定模型（关键）

**核心问题**：判定烟幕云团是否遮挡了导弹到真目标的全部视线。

**方法一：代表点法（实用）**
- 将圆柱目标离散为代表点集 {Tⱼ}（底面/顶面圆周上均匀取点，如 8~16 个）
- 对每个 Tⱼ，计算线段 M(t)→Tⱼ 到云心 C(t) 的最短距离 dⱼ(t)
- 若所有 dⱼ(t) ≤ 10 m 且最近点在线段上 → 完全遮蔽
- 遮蔽有效当且仅当：∀j, dⱼ(t) ≤ 10

**方法二：角覆盖法（严谨）**
- 从导弹 M(t) 观测，云团球体张角半锥角 α = arcsin(10 / |MC|)
- 目标所有边界点对云心方向的最大张角 θ_max
- 若 θ_max ≤ α → 云团完全覆盖目标视角 → 有效遮蔽

**有效遮蔽时长**：在 [t_d, t_d+20] 窗口内，满足遮蔽条件的总时间。

---

## 五、五个子问题的求解策略

### 问题 1：给定参数求遮蔽时长（正向计算）

**给定**：FY1 朝假目标方向、v=120 m/s、tᵣ=1.5 s、Δt_f=3.6 s

**求解步骤**：
1. 计算 FY1 轨迹 → 释放点 R = (17620, 0, 1800)
2. 计算炸弹弹道 → 起爆点 D = (17188, 0, 1736.5)
3. 计算云心轨迹 C(t) = D + (0,0,-3·(t-5.1))，t∈[5.1, 25.1]
4. 计算 M1 轨迹 M1(t)
5. 对 t ∈ [5.1, 25.1] 逐时刻判定遮蔽（步长如 0.01s）
6. 统计有效遮蔽总时长

**方法**：数值积分 / 时间步进扫描

### 问题 2：单弹优化（4 变量）

**决策变量**：θ（航向）, v（速度 70~140）, tᵣ（投放时刻）, Δt_f（引信延时）

**目标**：max 有效遮蔽时长 T

**策略**：
- **几何分析先行**：分析 M1 轨迹与真目标的视线随时间变化，确定最优云团位置应满足：
  - 云团在导弹-目标视线上
  - 云团有效窗口内视线扫过云团的时间最长
  - 考虑云团下沉（3 m/s）与导弹运动（300 m/s）的相对运动
- **反推参数**：由理想云团位置反推无人机航向、速度、投放时刻、引信延时
- **精化**：非线性优化（如 SLSQP、Nelder-Mead）或网格搜索 + 局部优化

### 问题 3：FY1 投 3 枚弹（8 变量）

**决策变量**：θ, v（FY1 共享）+ 每弹 {tᵣᵢ, Δt_fᵢ}, i=1,2,3
- 约束：|tᵣᵢ - tᵣⱼ| ≥ 1 s

**目标**：max T₁ + T₂ + T₃（遮蔽可不连续）

**策略**：
- 3 弹 × 20 s = 60 s 理论上限，M1 飞行 67 s
- 分时段覆盖：将导弹飞行分为早/中/晚期，每弹负责一个时段
- 先用问题 2 的方法确定每弹的理想起爆点位与时刻
- 再反推统一的 θ, v 使 3 弹均可达成
- 启发式优化（GA/PSO）联合求解 8 变量
- 输出 result1.xlsx

### 问题 4：3 架无人机各投 1 弹（12 变量）

**决策变量**：每架 {θᵢ, vᵢ, tᵣᵢ, Δt_fᵢ}, i=FY1,FY2,FY3

**策略**：
- 3 架无人机位置差异大（FY1 远、FY3 近），可覆盖不同时段
- 按距离/到达时间分配时段
- 各无人机独立优化 + 时段协调
- 输出 result2.xlsx

### 问题 5：5 架无人机抗 3 枚导弹（最复杂）

**决策变量**：
- 每架 {θᵢ, vᵢ} + 每弹 {tᵣᵢⱼ, Δt_fᵢⱼ, 干扰目标导弹编号}
- 5 机 × ≤3 弹 = ≤15 枚弹，分配给 3 枚导弹

**策略**：
1. **任务分配层**：确定每架无人机/每枚弹干扰哪枚导弹
   - 考虑无人机位置与导弹来袭方向的几何关系
   - 每枚导弹需 ~3 弹覆盖（3×20s ≈ 60s ≈ 飞行时间）
2. **参数优化层**：对每枚导弹的干扰弹群分别优化（类似问题 3）
3. **联合优化**：GA/PSO 多变量联合求解，或分层解耦
- 输出 result3.xlsx

---

## 六、整体方案框架

```
第一层：物理建模
├── 无人机运动（等高度匀速直线）
├── 炸弹弹道（平抛运动）
├── 烟幕云团（球状下沉 r=10 有效20s）
├── 导弹轨迹（匀速直线指向假目标）
└── 视线遮蔽判定（角覆盖法 / 代表点法）

第二层：遮蔽时长计算
├── 给定投放策略参数 → 正向计算有效遮蔽时长
└── 时间步进扫描（步长 0.01s）

第三层：优化求解
├── 问题1：正向计算（无优化）
├── 问题2：4变量非线性优化
├── 问题3：8变量 + 时序约束（GA/PSO）
├── 问题4：12变量多机协调
└── 问题5：任务分配 + 多变量联合优化

第四层：结果输出
├── result1.xlsx（问题3）
├── result2.xlsx（问题4）
└── result3.xlsx（问题5）
```

---

## 七、关键技术要点

1. **g 值取 9.8 m/s²**（题目未明确，需在论文中说明假设）
2. **遮蔽判定的严谨性**：目标为圆柱体（r=7, h=10），云团（r=10）大于目标半径，但仍需验证是否完全覆盖
3. **时间步长**：遮蔽判定建议 0.01s 步长，平衡精度与计算量
4. **无人机等高度飞行**："朝向假目标方向"指水平投影方向，非三维指向
5. **弹间间隔约束**：同一无人机投放间隔 ≥ 1 s
6. **速度范围**：70~140 m/s，航向 0~360°
7. **多弹遮蔽可不连续**：无需时间上连续覆盖，分别计算各弹遮蔽时长求和

---

## 八、论文写作建议

1. **摘要**：给出每个问题的方法和关键结果数值
2. **模型假设**：明确 g=9.8、忽略空气阻力、云团球状均匀等
3. **符号说明**：统一符号体系
4. **模型建立**：分运动学模型和遮蔽判定模型两部分
5. **模型求解**：问题1详细展示计算过程，问题2~5展示优化策略和结果
6. **结果验证**：用问题1的结果验证模型正确性
7. **附录**：全部可运行源代码（Python/MATLAB）
